[project]
name = "shudu"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numba>=0.60,<1",
    "numpy>=1.24,<3",
    "opencv-python-headless>=4.10,<5",
]

[dependency-groups]
dev = [
    "pytest>=8,<10",
]

[tool.uv]
index-url = "https://pypi.org/simple/"
